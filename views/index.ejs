<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>XBILLS</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #fff;
            background: linear-gradient(135deg, #4e79a7, #76b7b2);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        header {
            margin-bottom: 40px;
        }

        h1 {
            font-size: 3.5rem;
            margin-bottom: 20px;
        }

        p {
            font-size: 1.2rem;
            margin-bottom: 30px;
            line-height: 1.6;
            max-width: 700px;
        }

        a {
            text-decoration: none;
            color: #fff;
            background-color: #0099cc;
            padding: 12px 25px;
            border-radius: 5px;
            font-size: 1.1rem;
            font-weight: bold;
            transition: background-color 0.3s ease;
        }

        a:hover {
            background-color: #0077aa;
        }

        footer {
            margin-top: 50px;
            font-size: 0.9rem;
        }

        .features {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
        }

        .feature {
            background-color: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 10px;
            width: 250px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            cursor: pointer;
            position: relative;
        }

        .feature:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.3);
        }

        .feature h3 {
            margin-bottom: 10px;
            font-size: 1.3rem;
        }

        .feature p {
            font-size: 1rem;
            color: #e0e0e0;
        }

        .modal {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background: rgba(255, 255, 255, 0.9);
            color: #333;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.4);
            display: none;
            z-index: 1000;
        }

        .modal h3 {
            margin-bottom: 15px;
        }

        .modal p {
            font-size: 1rem;
            margin-bottom: 20px;
        }

        .modal-close {
            background: #0099cc;
            color: #fff;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
        }

        .modal-close:hover {
            background: #0077aa;
        }

        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            display: none;
            z-index: 999;
        }
    </style>
</head>
<body>
    <header>
        <h1>XBILLS Water Billing Solutions</h1>
        <p>Your ultimate solution for efficient and seamless water billing management. Track, bill, and manage with confidence.</p>
        <a href="/register">Get Started</a>
    </header>

    <section class="features">
        <div class="feature" data-modal="modal-efficient">
            <h3>Efficient Billing</h3>
            <p>Automate your water billing process and reduce manual errors with our advanced tools.</p>
        </div>
        <div class="feature" data-modal="modal-analytics">
            <h3>Data Analytics</h3>
            <p>Gain valuable insights into water usage trends with detailed reports and visualizations.</p>
        </div>
        <div class="feature" data-modal="modal-management">
            <h3>User Management</h3>
            <p>Easily manage client accounts, invoices, and payments on a single platform.</p>
        </div>
        <div class="feature" data-modal="modal-scalable">
            <h3>Scalable Solutions</h3>
            <p>Whether you're a small company or a large enterprise, our system scales with you.</p>
        </div>
    </section>

    <div class="overlay" id="overlay"></div>

    <div class="modal" id="modal-efficient">
        <h3>Efficient Billing</h3>
        <p>Our efficient billing system ensures accurate calculations, automated processes, and reduced operational costs, allowing your team to focus on more strategic goals.</p>
        <button class="modal-close">Close</button>
    </div>

    <div class="modal" id="modal-analytics">
        <h3>Data Analytics</h3>
        <p>Unlock powerful analytics to track water usage patterns, identify inefficiencies, and make data-driven decisions for optimal performance.</p>
        <button class="modal-close">Close</button>
    </div>

    <div class="modal" id="modal-management">
        <h3>User Management</h3>
        <p>Our intuitive user management interface allows easy administration of client accounts, payment tracking, and invoice generation.</p>
        <button class="modal-close">Close</button>
    </div>

    <div class="modal" id="modal-scalable">
        <h3>Scalable Solutions</h3>
        <p>Adapt our system to your growth, ensuring smooth operations whether managing a few clients or thousands.</p>
        <button class="modal-close">Close</button>
    </div>

    <footer>
        &copy; 2024 XBILLS Water Billing Solutions. All Rights Reserved.
    </footer>

    <script>
        const features = document.querySelectorAll('.feature');
        const modals = document.querySelectorAll('.modal');
        const overlay = document.getElementById('overlay');
        const closeButtons = document.querySelectorAll('.modal-close');

        features.forEach(feature => {
            feature.addEventListener('click', () => {
                const modalId = feature.getAttribute('data-modal');
                const modal = document.getElementById(modalId);
                modal.style.display = 'block';
                overlay.style.display = 'block';
            });
        });

        closeButtons.forEach(button => {
            button.addEventListener('click', () => {
                modals.forEach(modal => modal.style.display = 'none');
                overlay.style.display = 'none';
            });
        });

        overlay.addEventListener('click', () => {
            modals.forEach(modal => modal.style.display = 'none');
            overlay.style.display = 'none';
        });
    </script>
</body>
</html>
